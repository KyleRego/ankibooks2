<%= render partial: 'form', locals: {title: "Editing Article: #{@article.name}"} %>
<%= link_to "Delete article", book_article_path(@book, @article), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" } %>